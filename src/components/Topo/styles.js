import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#eee',
    padding: 16,
  },
  titulo: {
    marginTop: 24,
    color: '#121212',
    fontSize: 20,
    lineHeight: 42,
    fontWeight: 'bold',
  },
  subtitulo: {
    fontSize: 16,
    lineHeight: 26,
  },
});

export default styles;
